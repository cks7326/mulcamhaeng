import math # 모듈을 여러개 가져올 경우, 이용해도 된다.
# 실무에서는 모듈을 가져올 때 보통 하나의 import 에서 하나의 모듈을 정의 - 권장
# pip 는 Python 설치할 때 같이 설치 됨 - 외부, 상업용, 오픈소스의 모듈을 설치할 때 사용
# pip install 모듈이름
# 모듈 안에 함수를 사용할 때에는 모듈.함수()
data1 = math.factorial(10)

temp = 0